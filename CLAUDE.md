# test-agent-lab

에이전트에게 테스트를 쓰게 하고, 그 테스트가 실제로 버그를 잡는지 측정하는 실험실.

## 구조

```
agent/              에이전트 구현 (generate / repair / critique)
fixtures/sundaes/   실험 대상 앱 — 아래 "픽스처 불변 원칙" 참고
docs/               실행에서 나온 발견 기록
reports/            실행별 측정 결과
```

## 레포의 무게중심

이 레포의 산출물은 **실험 리포트**다. 범용 CLI 도구가 아니다.

`agent/` 는 실험 도구이지 제품이 아니므로, 다른 프로젝트에서도 쓰이도록
일반화하는 데 시간을 쓰지 않는다 — 설정 파일 체계, 타 프레임워크 지원,
사용자용 에러 처리 같은 것들. 이 분야는 이미 상용 도구가 많아서
일반화 비용이 값을 하지 못한다.

대신 `docs/` 와 `reports/` 에 투자한다. 남이 복제할 수 없는 건
도구가 아니라 **이 픽스처에서 실제로 관측된 숫자**다.

로드맵 5단계까지 끝난 뒤 여력이 되면 그때 CLI 로 뽑아낸다.
순서가 반대면 일반화에 쓴 시간이 회수되지 않는다.

## 픽스처 불변 원칙

`fixtures/sundaes/`는 **비교 기준**이다. 여기가 흔들리면 에이전트 실행 결과를
서로 비교할 수 없고, 성능 변화가 에이전트 탓인지 픽스처 탓인지 구분이 안 된다.

그래서:

- **소스(`src/**/*.tsx`, 테스트 제외)를 고치지 않는다.** 버그를 발견해도 고치지 않는다
- **의존성 메이저 버전을 올리지 않는다.** 업그레이드는 별도 실험으로 분리한다
- 예외는 *측정 자체를 불가능하게 만드는* 결함뿐이다 (예: 전체 실행이 빨간불)
  - 이 경우 `docs/`에 근거를 남기고 고친다

현재 의도적으로 남겨둔 결함 2건은 `docs/00-베이스라인-발견.md` 참고.
에이전트가 이걸 잡아내는지가 평가 지표라서 고치면 안 된다.

테스트 파일은 예외다. 에이전트가 지우고 다시 쓰는 게 실험의 본체다.
실험 후에는 반드시 `git checkout`으로 되돌린다.

## 측정 규칙

숫자는 항상 `reports/`에 남긴다. 대화에만 남은 숫자는 없는 것과 같다.

커버리지 하나만으로 결론짓지 않는다. 이 레포의 논지 자체가
"커버리지는 검증 강도를 말해주지 않는다"이므로, 커버리지를 근거로
"테스트가 좋아졌다"고 쓰면 자기모순이다. 뮤테이션 스코어와 함께 본다.

## 명령어

```bash
cd fixtures/sundaes
npm ci
npm run lint          # eslint, --max-warnings 0
npx tsc --noEmit
npm run test:coverage # vitest run --coverage
npm run test:e2e      # cypress (dev 서버 자동 기동)
```

## 커밋

`.gitmessage.txt`의 Conventional Commits 규약을 따른다. 본문은 한국어로 쓴다.
픽스처를 건드린 커밋은 `fix(fixture):` / `chore(fixture):`처럼 스코프를 붙여
루트 코드 변경과 구분한다.

무엇을 바꿨는지보다 **왜 바꿨는지**를 쓴다. diff는 이미 무엇을 말해준다.
